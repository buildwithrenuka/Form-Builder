# Copy this file to .env.local and fill in your values.
# .env.local is gitignored and loaded automatically by Vite.

# URL of the deployed API (Cloudflare Workers)
# Leave empty to use localStorage-only mode during development
VITE_API_URL=https://formquest-api.<your-subdomain>.workers.dev
