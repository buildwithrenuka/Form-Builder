{
  "name": "@myapp/api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler d1 migrations apply formquest-db --local && wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create formquest-db",
    "db:migrate:local": "wrangler d1 migrations apply formquest-db --local",
    "db:migrate": "wrangler d1 migrations apply formquest-db",
    "db:migrate:prod": "wrangler d1 migrations apply formquest-db --env production"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.3.3",
    "@myapp/shared": "*",
    "@scalar/hono-api-reference": "^0.5.0",
    "@trpc/server": "^11.0.0",
    "drizzle-orm": "^0.30.0",
    "hono": "^4.0.0",
    "jose": "^5.0.0",
    "zod": "^3.0.0",
    "zod-to-json-schema": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260521.1",
    "@types/node": "^20.0.0",
    "drizzle-kit": "^0.20.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.0.0"
  }
}
